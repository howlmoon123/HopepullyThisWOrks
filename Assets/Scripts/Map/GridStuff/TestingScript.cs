using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestingScript : MonoBehaviour
{
    MyGrid myGrid;
    // Start is called before the first frame update
    void Start()
    {
        myGrid = new MyGrid(4, 2, 10f);

        
    }

    
}
